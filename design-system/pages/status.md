# Status — overrides

Use MASTER. This page is the **holatia.app ops room**, forest full-bleed (not a green column on cream).

- `SiteNav` variant dark; `aria-current` on Status
- Service rows: name + human badge (En línea / En pausa / No configurado) + readable detail
- Contrato: truncated display + Copiar + Solscan
- Accesly: never print app id or env names
- TIA Agent (Prova): “Agente en pausa” if disabled — honest, not raw `disabled`
- Do not mark services live unless health/config says so
- Metrics stay the documented MVP facts (US→MX, 0.25%, WhatsApp, devnet) — no invented volume

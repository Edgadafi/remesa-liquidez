# Home — overrides

Use MASTER. This page is the **sales landing for GET /premium/fx**, cream surface.

- Max content width 960px; hero H1 serif, body sans
- Nav in header (Inicio · Comercio · Status)
- Primary job: convert a builder to hit the 402 (curl or open endpoint)
- Hero: USD/MXN live + **price from `/health`**, fallback `$0.10` / `$0.25` — no invented volume
- Pills: HTTP 402 / USDC Stellar / Bitso first; remesa chips secondary
- Primary CTA “Probar el 402” → `#premium-fx` + live `GET …/premium/fx`
- Secondary CTA “Copiar curl” (full command, not truncated hash)
- SKU cards: FX `$0.10` (lead) · puente `$0.25` (upsell)
- Proof: one Stellar Expert tx — no fake uptime
- Demo remesa (`#sender-app`) stays below the fold — “demo en devnet”, not the sale
- Human Spanish — no `NEXT_PUBLIC_*`, no facilitator key names
- Do not promise remesa to strangers, piloto Stellar, or recurrente
